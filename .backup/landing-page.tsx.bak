"use client";

import Link from "next/link";
import { Scale, ArrowRight, Sparkles, Search, FileEdit, TrendingUp, Shield } from "lucide-react";
import { motion } from "framer-motion";

const fade = (delay = 0) => ({
  initial: { opacity: 0, y: 16 },
  animate: { opacity: 1, y: 0 },
  transition: { duration: 0.5, delay, ease: [0.25, 0.4, 0.25, 1] },
});

export default function LandingPage() {
  return (
    <div className="min-h-screen bg-[#10182B] text-white selection:bg-blue-600 selection:text-white overflow-hidden">
      {/* Subtle grid background */}
      <div className="fixed inset-0 opacity-[0.03] bg-[linear-gradient(to_right,#fff_1px,transparent_1px),linear-gradient(to_bottom,#fff_1px,transparent_1px)] bg-[size:48px_48px] pointer-events-none" />

      {/* ─── Navbar ─── */}
      <motion.nav
        {...fade(0)}
        className="relative z-10 flex items-center justify-between px-6 lg:px-16 py-5"
      >
        <div className="flex items-center space-x-3">
          <div className="w-9 h-9 rounded-lg bg-blue-600 flex items-center justify-center shadow-lg shadow-blue-600/20">
            <Scale className="w-[18px] h-[18px] text-white" />
          </div>
          <div>
            <span className="font-bold tracking-tight text-[15px]">TRILEGAL AI</span>
            <span className="text-[11px] text-slate-400 ml-2 font-medium hidden sm:inline">Your AI Co-Counsel</span>
          </div>
        </div>
        <div className="flex items-center space-x-3">
          <Link
            href="/login"
            className="text-[13px] text-slate-300 hover:text-white transition-colors font-medium px-4 py-2"
          >
            Sign In
          </Link>
          <Link
            href="/login"
            className="text-[13px] bg-blue-600 hover:bg-blue-500 text-white font-semibold px-5 py-2 rounded-lg transition-colors shadow-sm shadow-blue-600/25"
          >
            Launch Demo →
          </Link>
        </div>
      </motion.nav>

      {/* ─── Hero ─── */}
      <section className="relative z-10 flex flex-col items-center text-center px-6 pt-20 lg:pt-32 pb-16">
        <motion.div {...fade(0.1)}>
          <span className="inline-flex items-center gap-2 text-[11px] font-semibold px-3 py-1 rounded-full bg-blue-950/60 text-blue-300 border border-blue-800/50 uppercase tracking-wider mb-8">
            <Sparkles className="w-3 h-3" />
            Enterprise Legal Operating System
          </span>
        </motion.div>

        <motion.h1
          {...fade(0.2)}
          className="text-4xl sm:text-5xl lg:text-6xl font-bold tracking-tight leading-[1.1] max-w-3xl"
        >
          Research. Draft.{" "}
          <br className="hidden sm:block" />
          Strategize.{" "}
          <span className="text-blue-400">Grow.</span>
        </motion.h1>

        <motion.p
          {...fade(0.3)}
          className="mt-6 text-[15px] sm:text-base text-slate-400 leading-relaxed max-w-xl"
        >
          Trilegal AI unifies legal research, AI-powered drafting, case risk
          analysis, and the <strong className="text-slate-200">OrbitX</strong> talent
          intelligence engine — in one co-counsel workspace built for Indian law
          firms.
        </motion.p>

        <motion.div {...fade(0.4)} className="flex flex-col sm:flex-row items-center gap-3 mt-10">
          <Link
            href="/login"
            className="inline-flex items-center gap-2 bg-blue-600 hover:bg-blue-500 text-white font-semibold text-[14px] px-7 py-3 rounded-lg transition-colors shadow-lg shadow-blue-600/20"
          >
            Enter Demo
            <ArrowRight className="w-4 h-4" />
          </Link>
          <span className="text-[12px] text-slate-500 font-medium">
            No sign-up required — pre-filled credentials
          </span>
        </motion.div>
      </section>

      {/* ─── Feature Cards ─── */}
      <section className="relative z-10 px-6 lg:px-16 pb-24 pt-8">
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 max-w-5xl mx-auto">
          {[
            {
              icon: Search,
              title: "Unified Research",
              desc: "SCC Online, Manupatra, Westlaw India & firm knowledge — searched together.",
            },
            {
              icon: FileEdit,
              title: "AI Drafting Studio",
              desc: "Generate petitions, notices and briefs grounded in cited precedent.",
            },
            {
              icon: Shield,
              title: "Case Risk Scoring",
              desc: "OrbitX analyses jurisdiction, bench composition and statutory exposure.",
            },
            {
              icon: TrendingUp,
              title: "OrbitX Growth",
              desc: "Track lawyer competencies, recommend assignments and build talent.",
            },
          ].map((f, i) => {
            const Icon = f.icon;
            return (
              <motion.div
                key={f.title}
                {...fade(0.3 + i * 0.08)}
                className="p-5 rounded-xl bg-white/[0.04] border border-white/[0.06] hover:border-white/[0.12] hover:bg-white/[0.06] transition-all group"
              >
                <div className="w-9 h-9 rounded-lg bg-blue-600/10 border border-blue-500/10 flex items-center justify-center mb-4 group-hover:bg-blue-600/15 transition-colors">
                  <Icon className="w-4 h-4 text-blue-400" />
                </div>
                <h3 className="text-[14px] font-semibold text-white tracking-tight">
                  {f.title}
                </h3>
                <p className="text-[12px] text-slate-400 leading-relaxed mt-1.5">
                  {f.desc}
                </p>
              </motion.div>
            );
          })}
        </div>
      </section>

      {/* ─── Footer ─── */}
      <footer className="relative z-10 border-t border-white/[0.06] px-6 lg:px-16 py-6 flex flex-col sm:flex-row items-center justify-between text-[11px] text-slate-500 gap-3">
        <span>© 2026 Trilegal AI · Demo Application</span>
        <span className="flex items-center gap-1.5">
          <span className="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse" />
          Demo Mode — Simulated Data
        </span>
      </footer>
    </div>
  );
}
